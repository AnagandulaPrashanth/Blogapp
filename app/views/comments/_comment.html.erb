  <tbody>
      <tr>
        <td><%= comment.commenter %></td>
        <td><%= comment.body %></td>
        <td><%= current_user.id %></td>
        <td><%= current_user.id if comment.user.present? %></td>
        <td><%= link_to 'Destroy Comment', [comment.post, comment],
               method: :delete,
               data: { confirm: 'Are you sure?' } %></td>
      </tr>
  </tbody>

